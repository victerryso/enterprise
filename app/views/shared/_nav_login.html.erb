<% if @current_user %>
  <%= link_to login_path, :data => {:method => :delete, :confirm => 'Really logout?'} do %>
    <h4>LOG OUT <%= @current_user.username.upcase %></h4>
  <% end %>
<% else %>
  <h4>LOG IN</h4>
  <div class="login_form">
  <%= form_tag '/login' do %>
    <p>
      <%= label_tag 'username' %>
      <%= text_field_tag 'username', nil %>
    </p>
    <p>
      <%= label_tag 'password' %>
      <%= password_field_tag 'password' %>
    </p>
    <%= submit_tag 'Login', :id =>"login_form_button"%>
    <%= link_to('Sign Up', new_user_path) %>
  <% end %>
  </div>
<% end %>
